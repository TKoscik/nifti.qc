write.qc.bex <- function(researcher, project, subject, session,
                         anat.img="T1w", suffix="bex") {
  #debug
  researcher <- "/Shared/nopoulos"
  project <- "sca_pilot"

  qc.dir <- paste0(researcher, "/", project, "/derivatives/qc/bex")
}
